import { userAccountDeletionWorkflowDTO, userIdDTO } from 'src/modules/users/users_dto';
export declare function userAccountDeletionWorkflow(user: userIdDTO): Promise<userAccountDeletionWorkflowDTO>;
